[project]
name = "get-access-token"
version = "0.1.0"
description = "A script to get access tokens from MS Graph Explorer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.58.0",
    "pyperclip>=1.11.0",
    "python-dotenv>=1.2.2",
]
